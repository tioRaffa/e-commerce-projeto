

    python manage.py shell

    
    from core.models import 'ex: ->' Category
--  categorias = Category.objects.all() -> seleciona todos os objetos dentro da classe
                                 .get(id=0)


    categorias.order_by('id' ou '-id')

        categoria_ultimo = categorias.order_by('-id').first()           -> PEGA O ULTIMO OBJETO
        categoria_primeiro = categorias.first()                         -> PEGA O PRIMEIRO OBJETO


    !!! NAO PODE SER QuerySet
    categoria._meta.get_fields()           -> mostra todos os atributos de classe


    >>> for receita in receitas : print(receita.id, receita.title) 
    ...
    1 Bolo de Morango
    2 Pão de Queijo
    3 Carne de porco
    >>>


    EDITAR:
        outra_coisa = Category.objects.get('id=1')
        
            |- outra_coisa.name = 'novo nome'